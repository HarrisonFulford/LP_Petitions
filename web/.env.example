# Public testnet RPC used by browser-side wagmi and public-safe API config.
NEXT_PUBLIC_BASE_SEPOLIA_RPC_URL=https://sepolia.base.org

# Fill these after Harrison deploys the demo contracts on Base Sepolia.
NEXT_PUBLIC_LP_PETITION_ADDRESS=
NEXT_PUBLIC_MOCK_NVDA_ADDRESS=
NEXT_PUBLIC_MOCK_NVDA_USD_AGGREGATOR_ADDRESS=

# Optional server-only override for receipt verification. Do not expose private RPC keys client-side.
BASE_SEPOLIA_RPC_URL=

# F6 auto-executor (server-only; NEVER NEXT_PUBLIC). Signs + submits execute().
EXECUTOR_PRIVATE_KEY=
# Protects GET /api/cron/sweep. Vercel Cron sends "Authorization: Bearer <CRON_SECRET>".
# Leave unset locally to call the sweep endpoint without auth.
CRON_SECRET=
# F6 Uniswap Swap API (optional pre-mint balancing swap calldata). The demo path mints
# at the Chainlink ratio, so this is only used when a rebalance is needed + key is set.
UNISWAP_API_KEY=
UNISWAP_API_BASE_URL=https://trade-api.gateway.uniswap.org/v1

# Optional hosted Postgres mirror. When absent, /api/petitions uses in-memory dev storage.
DATABASE_URL=

# F4 price fetching. xStocks docs route public asset price data through Backed's public API.
XSTOCKS_NVDA_PRICE_URL=https://api.backed.fi/api/v2/public/assets/NVDAx/price-data
XSTOCKS_QUOTE_ASSET_URL=https://api.xstocks.fi/api/v1/quotes/assets/NVDAx
# Optional timeout guard so a slow public endpoint cannot stall the demo. Defaults to 4000.
XSTOCKS_FETCH_TIMEOUT_MS=4000
# Optional server-only demo fallback if live xStocks public quotes are closed/null.
# This seeds the mock oracle for local demos; it is not live market data.
NVDA_USD_FALLBACK_PRICE=
